//modules
// import PropTypes from 'prop-types';

// styles
import styles from './Logo.module.css';

const Logo = () => (
  <a className={styles.Logo} href=" ">
    <p className={styles.LogoText}>ToDo</p>
  </a>
);

export default Logo;
